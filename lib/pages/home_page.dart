import 'package:flutter/material.dart';import '../p2p_connection.dart';import 'screen_page.dart';class HomePage extends StatefulWidget {  const HomePage({super.key});  @override  State<HomePage> createState() => _HomePageState();}class _HomePageState extends State<HomePage> {  final _roomCtrl = TextEditingController();  final _p2p = P2PConnection();  String _status = '就绪';  bool _connected = false;  @override  void initState() {    super.initState();    _p2p.onStateChange = (s) => setState(() => _status = s);    _p2p.onError = (e) => setState(() => _status = '错误: $e');  }  @override  void dispose() {    _roomCtrl.dispose();    _p2p.disconnect();    super.dispose();  }  void _startControl() async {    final room = _roomCtrl.text.trim();    if (room.isEmpty) return;    setState(() => _status = '正在连接...');
